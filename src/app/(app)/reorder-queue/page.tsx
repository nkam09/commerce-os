import { RestockForecasting } from "@/components/pages/inventory/restock-forecasting";

export const metadata = {
  title: "Restock Forecasting — Commerce OS",
};

export default function ReorderQueuePage() {
  return <RestockForecasting />;
}
